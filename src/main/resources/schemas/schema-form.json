{
  "type" : "object",
  "id" : "urn:jsonschema:io:gravitee:policy:eeas:configuration:EncryptionPolicyConfiguration",
  "properties" : {
    "key" : {
      "title": "key used for data ciphering",
      "description": "Key can be specified into API property and defined here with EL.",
      "type" : "string"
    },
    "pointers" : {
      "title": "Json field pointers",
      "description": "Comma separated Jsonpointers for defined target fields. '*' is used for traversing json arrays.",
      "type" : "string"
    }
  },
  "required": [
    "key",
    "pointers"
  ]
}